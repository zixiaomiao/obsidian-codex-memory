# Release Assets

Download the installer for your system and run it directly:

- macOS: `install-macos.command`
- Windows: `install-windows.cmd`

The installers clone or update `zixiaomiao/codian` into the user's plugin directory and register it in the Codex personal marketplace.
