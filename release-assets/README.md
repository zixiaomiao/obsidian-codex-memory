# Release Assets

Download the installer for your system and run it directly:

- macOS: `install-macos.command`
- Windows: `install-windows.cmd`

The installers sync the full repository into `~/.codex/skills/codin GitHub`, copy only the runtime skill files into `~/.codex/skills/codin`, and register Codin in the Codex personal marketplace.
