# Release Assets

Download the installer for your system and run it directly:

- macOS: `install-macos.command`
- Windows: `install-windows.cmd`

The installers clone or update `zixiaomiao/codian` into the user's plugin directory, copy `skills/codian` into the standalone Codex skill directory, and register Codian in the Codex personal marketplace.
